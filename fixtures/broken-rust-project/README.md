# Broken Rust project fixture

This standalone crate intentionally contains a failing addition test. It is an
input for manual PatchArena experiments, not a member of the PatchArena
workspace. Do not fix it in this repository; copy it into a disposable Git
repository and use `examples/rust-basic/task.yaml` as the benchmark task.
